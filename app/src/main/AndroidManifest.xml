<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.qianfanyun.ycdemo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:allowBackup">
        <activity android:name=".CoordinatorLayout.simple_behavior.SimpleBehaviorActivity"></activity>
        <activity android:name=".CoordinatorLayout.nested.CoordinatorNestActivity" />
        <activity android:name=".CoordinatorLayout.CoordinatorLayoutActivity" />
        <activity android:name=".NestedScroll.NestedScrollActivity" />
        <activity android:name=".CoordinatorLayout.webview.CoordinatorLayoutWebviewActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>